start dev doc tree
    testing
    asynchronization
    strategies for maintainability
gridfshandler
    does the static_url bug still apply?
    review StaticFileHandler docstring, am i doing this right?
        note that GridFSHandler doesn't inherit from StaticFileHandler
    move to separate package?
search TODOs in code
update ha_tools again
where explicit Futures are constructed, use TracebackFuture?
tests
    time each test, fix slowest ones
doctests have to pass
    put them in Jenkins
no random logging and warnings in tests
tornado.util.ArgReplacer!!
last minute: move incidental logic to utils.py
run test_no_ssl